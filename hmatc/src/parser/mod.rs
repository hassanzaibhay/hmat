//! HMAT Parser (stub).
//!
//! The parser consumes the token stream produced by [`crate::lexer`] and
//! builds an abstract syntax tree as defined in [`crate::ast`].
//!
//! Not yet implemented — Phase 0 scope is the lexer only. This module exists
//! so the workspace layout, build graph, and `mod` declarations are in place
//! for the next session.
